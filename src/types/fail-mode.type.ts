export enum FailMode {
  /** Fail unless at least one input is present. */
  ANY = "any",

  /** Fail unless at least one input is present. */
  ALL = "all",

  /** Never fail. */
  NEVER = "never",
}
